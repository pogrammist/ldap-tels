@model IEnumerable<ldap_tels.Models.Division>
@{
    ViewData["Title"] = "Подразделения";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>Подразделения</h1>
    <div>
        <a asp-action="Contacts" class="btn btn-primary">
            <i class="bi bi-people"></i> Все контакты
        </a>
    </div>
</div>

@Html.AntiForgeryToken()
<input type="hidden" id="update-division-weight-url" value="@Url.Action("UpdateDivisionWeight", "Home", new { area = "Admin" })" />

@if (!Model.Any())
{
    <div class="alert alert-info">
        Подразделения не найдены.
    </div>
}
else
{
    <div class="card mb-4">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Подразделение</th>
                            <th>Приоритет</th>
                            <th>Действия</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var division in Model)
                        {
                            <tr>
                                <td>@division.Id</td>
                                <td>@division.Name</td>
                                <td>
                                    <div class="d-flex align-items-center">
                                        <button type="button" class="btn btn-sm btn-outline-secondary me-2" 
                                                onclick="changeDivisionWeight(@division.Id, -1)" 
                                                @(division.Weight <= 0 ? "disabled" : "")>
                                            <i class="bi bi-dash"></i>
                                        </button>
                                        
                                        <span id="division-weight-@division.Id" class="fw-bold">@division.Weight</span>
                                        
                                        <button type="button" class="btn btn-sm btn-outline-secondary ms-2" 
                                                onclick="changeDivisionWeight(@division.Id, 1)" 
                                                @(division.Weight >= 100 ? "disabled" : "")>
                                            <i class="bi bi-plus"></i>
                                        </button>
                                    </div>
                                </td>
                                <td>
                                    <a asp-action="Division" asp-route-division="@division.Name" class="btn btn-sm btn-info">
                                        <i class="bi bi-people"></i>
                                    </a>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
}

<div class="mt-4">
    <a asp-action="Index" class="btn btn-secondary">Вернуться на главную</a>
</div>

@section Scripts {
    <script src="~/js/divisions.js" asp-append-version="true"></script>
}