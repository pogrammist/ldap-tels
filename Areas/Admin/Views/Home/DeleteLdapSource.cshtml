@model ad_tels.Models.LdapSource

@{
    ViewData["Title"] = "Удаление LDAP-источника";
}

<div class="container">
    <h1 class="mb-4">Удаление LDAP-источника</h1>

    <div class="alert alert-danger">
        <h4 class="alert-heading">Внимание!</h4>
        <p>Вы уверены, что хотите удалить этот LDAP-источник? Это действие нельзя будет отменить.</p>
        <p>Все контакты, связанные с этим источником, также будут удалены.</p>
    </div>

    <div class="card mb-4">
        <div class="card-header">
            <h5>Информация об источнике</h5>
        </div>
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">ID</dt>
                <dd class="col-sm-9">@Model.Id</dd>

                <dt class="col-sm-3">Название</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">Сервер</dt>
                <dd class="col-sm-9">@Model.Server</dd>

                <dt class="col-sm-3">Порт</dt>
                <dd class="col-sm-9">@Model.Port</dd>

                <dt class="col-sm-3">Base DN</dt>
                <dd class="col-sm-9">@Model.BaseDn</dd>

                <dt class="col-sm-3">Статус</dt>
                <dd class="col-sm-9">
                    @if (Model.IsActive)
                    {
                        <span class="badge bg-success">Активен</span>
                    }
                    else
                    {
                        <span class="badge bg-danger">Неактивен</span>
                    }
                </dd>

                <dt class="col-sm-3">Последняя синхронизация</dt>
                <dd class="col-sm-9">@Model.LastSyncTime.ToString("dd.MM.yyyy HH:mm:ss")</dd>
            </dl>
        </div>
    </div>

    <form asp-area="Admin" asp-controller="Home" asp-action="DeleteLdapSource" method="post">
        <input type="hidden" asp-for="Id" />
        <div class="form-group">
            <button type="submit" class="btn btn-danger">Удалить</button>
            <a asp-area="Admin" asp-controller="Home" asp-action="LdapSources" class="btn btn-secondary">Отмена</a>
        </div>
    </form>
</div>
