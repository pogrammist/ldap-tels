@model IEnumerable<ldap_tels.Models.Contact>

@{
    ViewData["Title"] = "Контакты";
}

<div class="container">
    <h1 class="mb-4">Контакты</h1>

    <div class="row mb-4">
        <div class="col-md-6">
            <form asp-area="Admin" asp-controller="Home" asp-action="SearchContacts" method="get" class="d-flex">
                <input type="text" name="query" class="form-control me-2" placeholder="Поиск по имени, отделу, телефону..." value="@ViewBag.SearchQuery">
                <button type="submit" class="btn btn-primary">Поиск</button>
            </form>
        </div>
        <div class="col-md-6 text-end">
            <a asp-area="Admin" asp-controller="Home" asp-action="Departments" class="btn btn-outline-secondary">Отделы</a>
        </div>
    </div>

    @if (ViewBag.Department != null)
    {
        <div class="alert alert-info">
            Отдел: @ViewBag.Department
            <a asp-area="Admin" asp-controller="Home" asp-action="Contacts" class="float-end">Сбросить фильтр</a>
        </div>
    }

    @if (ViewBag.SearchQuery != null)
    {
        <div class="alert alert-info">
            Результаты поиска для: "@ViewBag.SearchQuery"
            <a asp-area="Admin" asp-controller="Home" asp-action="Contacts" class="float-end">Сбросить поиск</a>
        </div>
    }

    @if (!Model.Any())
    {
        <div class="alert alert-warning">
            Контакты не найдены.
        </div>
    }
    else
    {
        <div class="table-responsive">
            <table class="table table-striped table-hover">
                <thead>
                    <tr>
                        <th>ФИО</th>
                        <th>Отдел</th>
                        <th>Должность</th>
                        <th>Телефон</th>
                        <th>Мобильный</th>
                        <th>Email</th>
                        <th>Источник</th>
                        <th>Действия</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var contact in Model)
                    {
                        <tr>
                            <td>@contact.DisplayName</td>
                            <td>
                                <a asp-area="Admin" asp-controller="Home" asp-action="ContactsByDepartment" asp-route-department="@contact.Department">
                                    @contact.Department
                                </a>
                            </td>
                            <td>@contact.Title</td>
                            <td>@contact.PhoneNumber</td>
                            <td>@contact.Mobile</td>
                            <td>
                                @if (!string.IsNullOrEmpty(contact.Email))
                                {
                                    <a href="mailto:@contact.Email">@contact.Email</a>
                                }
                            </td>
                            <td>
                                @if (contact.LdapSource != null)
                                {
                                    <a asp-area="Admin" asp-controller="Home" asp-action="LdapSourceDetails" asp-route-id="@contact.LdapSourceId">
                                        @contact.LdapSource.Name
                                    </a>
                                }
                                else
                                {
                                    <span class="text-muted">Нет</span>
                                }
                            </td>
                            <td>
                                <a asp-area="Admin" asp-controller="Home" asp-action="ContactDetails" asp-route-id="@contact.Id" class="btn btn-sm btn-info">
                                    <i class="bi bi-eye"></i>
                                </a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>

        @if (ViewBag.TotalPages > 1)
        {
            <nav aria-label="Page navigation">
                <ul class="pagination justify-content-center">
                    @if (ViewBag.CurrentPage > 1)
                    {
                        <li class="page-item">
                            <a class="page-link" 
                               asp-area="Admin"
                               asp-controller="Home"
                               asp-action="@(ViewBag.SearchQuery != null ? "SearchContacts" : (ViewBag.Department != null ? "ContactsByDepartment" : "Contacts"))" 
                               asp-route-page="1" 
                               asp-route-query="@ViewBag.SearchQuery"
                               asp-route-department="@ViewBag.Department">
                                &laquo;
                            </a>
                        </li>
                        <li class="page-item">
                            <a class="page-link" 
                               asp-area="Admin"
                               asp-controller="Home"
                               asp-action="@(ViewBag.SearchQuery != null ? "SearchContacts" : (ViewBag.Department != null ? "ContactsByDepartment" : "Contacts"))" 
                               asp-route-page="@(ViewBag.CurrentPage - 1)" 
                               asp-route-query="@ViewBag.SearchQuery"
                               asp-route-department="@ViewBag.Department">
                                &lt;
                            </a>
                        </li>
                    }

                    @for (int i = Math.Max(1, ViewBag.CurrentPage - 2); i <= Math.Min(ViewBag.TotalPages, ViewBag.CurrentPage + 2); i++)
                    {
                        <li class="page-item @(i == ViewBag.CurrentPage ? "active" : "")">
                            <a class="page-link" 
                               asp-area="Admin"
                               asp-controller="Home"
                               asp-action="@(ViewBag.SearchQuery != null ? "SearchContacts" : (ViewBag.Department != null ? "ContactsByDepartment" : "Contacts"))" 
                               asp-route-page="@i" 
                               asp-route-query="@ViewBag.SearchQuery"
                               asp-route-department="@ViewBag.Department">
                                @i
                            </a>
                        </li>
                    }

                    @if (ViewBag.CurrentPage < ViewBag.TotalPages)
                    {
                        <li class="page-item">
                            <a class="page-link" 
                               asp-area="Admin"
                               asp-controller="Home"
                               asp-action="@(ViewBag.SearchQuery != null ? "SearchContacts" : (ViewBag.Department != null ? "ContactsByDepartment" : "Contacts"))" 
                               asp-route-page="@(ViewBag.CurrentPage + 1)" 
                               asp-route-query="@ViewBag.SearchQuery"
                               asp-route-department="@ViewBag.Department">
                                &gt;
                            </a>
                        </li>
                        <li class="page-item">
                            <a class="page-link" 
                               asp-area="Admin"
                               asp-controller="Home"
                               asp-action="@(ViewBag.SearchQuery != null ? "SearchContacts" : (ViewBag.Department != null ? "ContactsByDepartment" : "Contacts"))" 
                               asp-route-page="@ViewBag.TotalPages" 
                               asp-route-query="@ViewBag.SearchQuery"
                               asp-route-department="@ViewBag.Department">
                                &raquo;
                            </a>
                        </li>
                    }
                </ul>
            </nav>
        }
    }
</div>
